const Allergen = require('./Allergen');
const Recipe = require('./Recipe');

Allergen.hasMany(Recipe, {
  foreignKey: 'allergen_id',
  onDelete: 'CASCADE'
});

Recipe.hasMany(Allergen, {
  foreignKey: 'recipe_id',
});

module.exports = { Allergen, Recipe };

Need to test this on live code! This is a many to many relationship between Recipe and Allergen